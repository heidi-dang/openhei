---
title: CLI
description: Options et commandes de la CLI OpenHei.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI OpenHei démarre par défaut le [TUI](/docs/tui) lorsqu'il est exécuté sans aucun argument.

```bash
openhei
```

Mais elle accepte également les commandes documentées sur cette page. Cela vous permet d'interagir avec OpenHei par programme.

```bash
openhei run "Explain how closures work in JavaScript"
```

---

### tui

Démarrez l’interface utilisateur du terminal OpenHei.

```bash
openhei [project]
```

#### Options

| Option       | Court | Description                                                                   |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continuer la dernière session                                                 |
| `--session`  | `-s`  | ID de session pour continuer                                                  |
| `--fork`     |       | Forker la session en continuant (à utiliser avec `--continue` ou `--session`) |
| `--prompt`   |       | Prompt à utiliser                                                             |
| `--model`    | `-m`  | Modèle à utiliser sous forme de fournisseur/modèle                            |
| `--agent`    |       | Agent à utiliser                                                              |
| `--port`     |       | Port d'écoute                                                                 |
| `--hostname` |       | Nom d'hôte d'écoute                                                           |

---

## Commandes

La CLI OpenHei dispose également des commandes suivantes.

---

### agent

Gérer les agents pour OpenHei.

```bash
openhei agent [command]
```

---

### attach

Attachez un terminal à un serveur backend OpenHei déjà en cours d'exécution démarré via les commandes `serve` ou `web`.

```bash
openhei attach [url]
```

Cela permet d'utiliser le TUI avec un backend OpenHei distant. Par exemple :

```bash
# Start the backend server for web/mobile access
openhei web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
openhei attach http://10.20.30.40:4096
```

#### Options

| Option      | Court | Description                                    |
| ----------- | ----- | ---------------------------------------------- |
| `--dir`     |       | Répertoire de travail dans lequel démarrer TUI |
| `--session` | `-s`  | ID de session pour continuer                   |

---

#### create

Créez un nouvel agent avec une configuration personnalisée.

```bash
openhei agent create
```

Cette commande vous guidera dans la création d'un nouvel agent avec un prompt système personnalisé et une configuration d'outils.

---

#### list

Liste tous les agents disponibles.

```bash
openhei agent list
```

---

### auth

Commande pour gérer les informations d’identification et la connexion des fournisseurs.

```bash
openhei auth [command]
```

---

#### login

OpenHei est alimenté par la liste des fournisseurs sur [Models.dev](https://models.dev), vous pouvez donc utiliser `openhei auth login` pour configurer les clés API pour tout fournisseur que vous souhaitez utiliser. Ceci est stocké dans `~/.local/share/openhei/auth.json`.

```bash
openhei auth login
```

Lorsque OpenHei démarre, il charge les fournisseurs à partir du fichier d'informations d'identification, ainsi que les clés définies dans vos environnements ou un fichier `.env` dans votre projet.

---

#### list

Liste tous les fournisseurs authentifiés tels qu'ils sont stockés dans le fichier d'informations d'identification.

```bash
openhei auth list
```

Ou la version courte.

```bash
openhei auth ls
```

---

#### logout

Vous déconnecte d'un fournisseur en l'effaçant du fichier d'informations d'identification.

```bash
openhei auth logout
```

---

### github

Gérez l'agent GitHub pour l'automatisation du dépôt.

```bash
openhei github [command]
```

---

#### install

Installez l'agent GitHub dans votre dépôt.

```bash
openhei github install
```

Cela configure le flux de travail GitHub Actions nécessaire et vous guide tout au long du processus de configuration. [En savoir plus](/docs/github).

---

#### run

Exécutez l'agent GitHub. Ceci est généralement utilisé dans les GitHub Actions.

```bash
openhei github run
```

##### Options

| Option    | Description                                          |
| --------- | ---------------------------------------------------- |
| `--event` | Événement simulé GitHub pour lequel exécuter l'agent |
| `--token` | Jeton d'accès personnel GitHub                       |

---

### MCP

Gérer les serveurs Model Context Protocol.

```bash
openhei mcp [command]
```

---

#### add

Ajoutez un serveur MCP à votre configuration.

```bash
openhei mcp add
```

Cette commande vous guidera dans l’ajout d’un serveur MCP local ou distant.

---

#### list

Liste tous les serveurs MCP configurés et leur état de connexion.

```bash
openhei mcp list
```

Ou utilisez la version courte.

```bash
openhei mcp ls
```

---

#### auth

Authentifiez-vous auprès d'un serveur MCP compatible OAuth.

```bash
openhei mcp auth [name]
```

Si vous ne fournissez pas de nom de serveur, vous serez invité à sélectionner parmi les serveurs compatibles OAuth disponibles.

Vous pouvez également répertorier les serveurs compatibles OAuth et leur statut d'authentification.

```bash
openhei mcp auth list
```

Ou utilisez la version courte.

```bash
openhei mcp auth ls
```

---

#### logout

Supprimez les informations d'identification OAuth pour un serveur MCP.

```bash
openhei mcp logout [name]
```

---

#### debug

Déboguer les problèmes de connexion OAuth pour un serveur MCP.

```bash
openhei mcp debug <name>
```

---

### models

Liste tous les modèles disponibles auprès des fournisseurs configurés.

```bash
openhei models [provider]
```

Cette commande affiche tous les modèles disponibles parmi vos fournisseurs configurés au format `provider/model`.

Ceci est utile pour déterminer le nom exact du modèle à utiliser dans [votre config](/docs/config/).

Vous pouvez éventuellement transmettre un ID de fournisseur pour filtrer les modèles en fonction de ce fournisseur.

```bash
openhei models anthropic
```

#### Options

| Option      | Description                                                                                |
| ----------- | ------------------------------------------------------------------------------------------ |
| `--refresh` | Actualisez le cache des modèles à partir de models.dev                                     |
| `--verbose` | Utiliser une sortie de modèle plus détaillée (inclut des métadonnées telles que les coûts) |

Utilisez l'option `--refresh` pour mettre à jour la liste des modèles mis en cache. Ceci est utile lorsque de nouveaux modèles ont été ajoutés à un fournisseur et que vous souhaitez les voir dans OpenHei.

```bash
openhei models --refresh
```

---

### run

Exécutez openhei en mode non interactif en transmettant directement un prompt.

```bash
openhei run [message..]
```

Ceci est utile pour les scripts, l'automatisation ou lorsque vous souhaitez une réponse rapide sans lancer le TUI complet. Par exemple :

```bash "openhei run"
openhei run Explain the use of context in Go
```

Vous pouvez également vous connecter à une instance `openhei serve` en cours d'exécution pour éviter les temps de démarrage à froid du serveur MCP à chaque exécution :

```bash
# Start a headless server in one terminal
openhei serve

# In another terminal, run commands that attach to it
openhei run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Options

| Option       | Court | Description                                                                                    |
| ------------ | ----- | ---------------------------------------------------------------------------------------------- |
| `--command`  |       | La commande à exécuter, utilisez le message pour les arguments                                 |
| `--continue` | `-c`  | Continuer la dernière session                                                                  |
| `--session`  | `-s`  | ID de session pour continuer                                                                   |
| `--fork`     |       | Forker la session en continuant (à utiliser avec `--continue` ou `--session`)                  |
| `--share`    |       | Partager la session                                                                            |
| `--model`    | `-m`  | Modèle à utiliser sous forme de fournisseur/modèle                                             |
| `--agent`    |       | Agent à utiliser                                                                               |
| `--file`     | `-f`  | Fichier(s) à joindre au message                                                                |
| `--format`   |       | Format : par défaut (formaté) ou json (événements JSON bruts)                                  |
| `--title`    |       | Titre de la session (utilise un prompt tronqué si aucune valeur n'est fournie)                 |
| `--attach`   |       | Connectez-vous à un serveur openhei en cours d'exécution (par exemple, http://localhost:4096) |
| `--port`     |       | Port du serveur local (port aléatoire par défaut)                                              |

---

### serve

Démarrez un serveur OpenHei sans interface graphique pour un accès API. Consultez le [serveur docs](/docs/server) pour l'interface complète HTTP.

```bash
openhei serve
```

Cela démarre un serveur HTTP qui fournit un accès API aux fonctionnalités d'OpenHei sans l'interface TUI. Définissez `OPENHEI_SERVER_PASSWORD` pour activer l'authentification de base HTTP (le nom d'utilisateur par défaut est `openhei`).

#### Options

| Option       | Description                                                |
| ------------ | ---------------------------------------------------------- |
| `--port`     | Port d'écoute                                              |
| `--hostname` | Nom d'hôte d'écoute                                        |
| `--mdns`     | Activer la découverte mDNS                                 |
| `--cors`     | Origines de navigateur supplémentaires pour autoriser CORS |

---

### session

Gérer les sessions OpenHei.

```bash
openhei session [command]
```

---

#### list

Liste toutes les sessions OpenHei.

```bash
openhei session list
```

##### Options

| Option        | Court | Description                                  |
| ------------- | ----- | -------------------------------------------- |
| `--max-count` | `-n`  | Limiter aux N sessions les plus récentes     |
| `--format`    |       | Format de sortie : tableau ou json (tableau) |

---

### stats

Affichez les statistiques d'utilisation et de coût des jetons pour vos sessions OpenHei.

```bash
openhei stats
```

#### Options

| Option      | Description                                                                                                           |
| ----------- | --------------------------------------------------------------------------------------------------------------------- |
| `--days`    | Afficher les statistiques des N derniers jours (depuis le début)                                                      |
| `--tools`   | Nombre d'outils à afficher (tous)                                                                                     |
| `--models`  | Afficher la répartition de l'utilisation du modèle (masqué par défaut). Passez un numéro pour afficher les N premiers |
| `--project` | Filtrer par projet (tous les projets, chaîne vide : projet actuel)                                                    |

---

### export

Exportez les données de session au format JSON.

```bash
openhei export [sessionID]
```

Si vous ne fournissez pas d'ID de session, vous serez invité à sélectionner parmi les sessions disponibles.

---

### import

Importez les données de session à partir d'un fichier JSON ou d'une URL de partage OpenHei.

```bash
openhei import <file>
```

Vous pouvez importer à partir d'un fichier local ou d'une URL de partage OpenHei.

```bash
openhei import session.json
openhei import https://opncd.ai/s/abc123
```

---

### web

Démarrez un serveur OpenHei sans interface graphique avec une interface Web.

```bash
openhei web
```

Cela démarre un serveur HTTP et ouvre un navigateur Web pour accéder à OpenHei via une interface Web. Définissez `OPENHEI_SERVER_PASSWORD` pour activer l'authentification de base HTTP (le nom d'utilisateur par défaut est `openhei`).

#### Options

| Option       | Description                                                |
| ------------ | ---------------------------------------------------------- |
| `--port`     | Port d'écoute                                              |
| `--hostname` | Nom d'hôte d'écoute                                        |
| `--mdns`     | Activer la découverte mDNS                                 |
| `--cors`     | Origines de navigateur supplémentaires pour autoriser CORS |

---

### acp

Démarrez un serveur ACP (Agent Client Protocol).

```bash
openhei acp
```

Cette commande démarre un serveur ACP qui communique via stdin/stdout en utilisant nd-JSON.

#### Options

| Option       | Description           |
| ------------ | --------------------- |
| `--cwd`      | Répertoire de travail |
| `--port`     | Port d'écoute         |
| `--hostname` | Nom d'hôte d'écoute   |

---

### uninstall

Désinstallez OpenHei et supprimez tous les fichiers associés.

```bash
openhei uninstall
```

#### Options

| Option          | Court | Description                                         |
| --------------- | ----- | --------------------------------------------------- |
| `--keep-config` | `-c`  | Conserver les fichiers de configuration             |
| `--keep-data`   | `-d`  | Conserver les données de session et les instantanés |
| `--dry-run`     |       | Afficher ce qui serait supprimé sans supprimer      |
| `--force`       | `-f`  | Ignorer les invites de confirmation                 |

---

### upgrade

Met à jour openhei vers la dernière version ou une version spécifique.

```bash
openhei upgrade [target]
```

Pour mettre à niveau vers la dernière version.

```bash
openhei upgrade
```

Pour passer à une version spécifique.

```bash
openhei upgrade v0.1.48
```

#### Options

| Option     | Court | Description                                                     |
| ---------- | ----- | --------------------------------------------------------------- |
| `--method` | `-m`  | La méthode d'installation utilisée ; curl, npm, pnpm, bun, brew |

---

## Options globales

La CLI openhei prend les flags globaux suivants.

| Option         | Court | Description                              |
| -------------- | ----- | ---------------------------------------- |
| `--help`       | `-h`  | Afficher l'aide                          |
| `--version`    | `-v`  | Afficher le numéro de version            |
| `--print-logs` |       | Afficher les logs sur stderr             |
| `--log-level`  |       | Niveau de log (DEBUG, INFO, WARN, ERROR) |

---

## Variables d'environnement

OpenHei peut être configuré à l'aide de variables d'environnement.

| Variables                             | Type    | Description                                                                       |
| ------------------------------------- | ------- | --------------------------------------------------------------------------------- |
| `OPENHEI_AUTO_SHARE`                 | booléen | Partager automatiquement des sessions                                             |
| `OPENHEI_GIT_BASH_PATH`              | chaîne  | Chemin vers l'exécutable Git Bash sur Windows                                     |
| `OPENHEI_CONFIG`                     | chaîne  | Chemin d'accès au fichier de configuration                                        |
| `OPENHEI_CONFIG_DIR`                 | chaîne  | Chemin d'accès au répertoire de configuration                                     |
| `OPENHEI_CONFIG_CONTENT`             | chaîne  | Contenu de configuration JSON en ligne                                            |
| `OPENHEI_DISABLE_AUTOUPDATE`         | booléen | Désactiver les vérifications automatiques des mises à jour                        |
| `OPENHEI_DISABLE_PRUNE`              | booléen | Désactiver le nettoyage des anciennes données                                     |
| `OPENHEI_DISABLE_TERMINAL_TITLE`     | booléen | Désactiver les mises à jour automatiques des titres du terminal                   |
| `OPENHEI_PERMISSION`                 | chaîne  | Configuration des autorisations JSON intégrées                                    |
| `OPENHEI_DISABLE_DEFAULT_PLUGINS`    | booléen | Désactiver les plugins par défaut                                                 |
| `OPENHEI_DISABLE_LSP_DOWNLOAD`       | booléen | Désactiver les téléchargements automatiques du serveur LSP                        |
| `OPENHEI_ENABLE_EXPERIMENTAL_MODELS` | booléen | Activer les modèles expérimentaux                                                 |
| `OPENHEI_DISABLE_AUTOCOMPACT`        | booléen | Désactiver le compactage automatique du contexte                                  |
| `OPENHEI_DISABLE_CLAUDE_CODE`        | booléen | Désactiver la lecture de `.claude` (prompt + compétences)                         |
| `OPENHEI_DISABLE_CLAUDE_CODE_PROMPT` | booléen | Désactiver la lecture `~/.claude/CLAUDE.md`                                       |
| `OPENHEI_DISABLE_CLAUDE_CODE_SKILLS` | booléen | Désactiver le chargement de `.claude/skills`                                      |
| `OPENHEI_DISABLE_MODELS_FETCH`       | booléen | Désactiver la récupération de modèles à partir de sources distantes               |
| `OPENHEI_FAKE_VCS`                   | chaîne  | Faux fournisseur VCS à des fins de test                                           |
| `OPENHEI_DISABLE_FILETIME_CHECK`     | booléen | Désactiver la vérification de l'heure des fichiers pour l'optimisation            |
| `OPENHEI_CLIENT`                     | chaîne  | Identifiant du client (par défaut `cli`)                                          |
| `OPENHEI_ENABLE_EXA`                 | booléen | Activer les outils de recherche Web Exa                                           |
| `OPENHEI_SERVER_PASSWORD`            | chaîne  | Activer l'authentification de base pour `serve`/`web`                             |
| `OPENHEI_SERVER_USERNAME`            | chaîne  | Remplacer le nom d'utilisateur d'authentification de base (par défaut `openhei`) |
| `OPENHEI_MODELS_URL`                 | chaîne  | URL personnalisé pour récupérer la configuration des modèles                      |

---

### Expérimental

Ces variables d'environnement activent des fonctionnalités expérimentales qui peuvent être modifiées ou supprimées.

| Variables                                       | Type    | Description                                                     |
| ----------------------------------------------- | ------- | --------------------------------------------------------------- |
| `OPENHEI_EXPERIMENTAL`                         | booléen | Activer toutes les fonctionnalités expérimentales               |
| `OPENHEI_EXPERIMENTAL_ICON_DISCOVERY`          | booléen | Activer la découverte d'icônes                                  |
| `OPENHEI_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | booléen | Désactiver la copie lors de la sélection dans TUI               |
| `OPENHEI_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | numéro  | Délai d'expiration par défaut pour les commandes bash en ms     |
| `OPENHEI_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | numéro  | Nombre maximum de jetons de sortie pour les réponses LLM        |
| `OPENHEI_EXPERIMENTAL_FILEWATCHER`             | booléen | Activer l'observateur de fichiers pour l'ensemble du répertoire |
| `OPENHEI_EXPERIMENTAL_OXFMT`                   | booléen | Activer le formateur oxfmt                                      |
| `OPENHEI_EXPERIMENTAL_LSP_TOOL`                | booléen | Activer l'outil expérimental LSP                                |
| `OPENHEI_EXPERIMENTAL_DISABLE_FILEWATCHER`     | booléen | Désactiver l'observateur de fichiers                            |
| `OPENHEI_EXPERIMENTAL_EXA`                     | booléen | Activer les fonctionnalités Exa expérimentales                  |
| `OPENHEI_EXPERIMENTAL_LSP_TY`                  | booléen | Activer la vérification expérimentale de type LSP               |
| `OPENHEI_EXPERIMENTAL_MARKDOWN`                | booléen | Activer les fonctionnalités Markdown expérimentales             |
| `OPENHEI_EXPERIMENTAL_PLAN_MODE`               | booléen | Activer le mode plan                                            |
