Antigravity (Google) OAuth plugin for OpenHei

This plugin enables OpenHei to authenticate with Google/Antigravity via OAuth and store multiple Google accounts for quota rotation.

Usage:

1. Add to your `~/.config/openhei/opencode.json` plugin list or install as package.
2. Run `openhei auth login` and choose `google`.
